window.onload = init;
function init(){

    $('#fitCenter img').ImageShowModeSet({
        imagemode:'fitCenter',
    });

    $('#fitStart img').ImageShowModeSet({
        imagemode:'fitStart',
    });

    $('#fitEnd img').ImageShowModeSet({
        imagemode:'fitEnd',
    });

    $('#fillCenter img').ImageShowModeSet({
        imagemode:'fillCenter',
    });

    $('#fillStart img').ImageShowModeSet({
        imagemode:'fillStart',
    });
    $('#fillEnd img').ImageShowModeSet({
        imagemode:'fillEnd',
    });
}